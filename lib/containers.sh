#!/usr/bin/env bash

# Format-preserving application-container policy boundary. The implementation
# is part of the checked-in static engine.
[[ ${HARDCORE_CONTAINERS_SH_LOADED:-0} == 1 ]] && return 0
HARDCORE_CONTAINERS_SH_LOADED=1
